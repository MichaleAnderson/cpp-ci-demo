name: C++ CI Pipeline (CentOS 7 Compatible)

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: centos:7

    steps:
      - name: Install Tools (GCC, CMake, Make)
        run: |
          yum -y update
          yum -y install centos-release-scl
          yum -y install devtoolset-11 gcc gcc-c++ make

      - name: Enable GCC Toolset
        run: echo "source /opt/rh/devtoolset-11/enable" >> ~/.bashrc

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Program
        run: |
          source /opt/rh/devtoolset-11/enable
          mkdir -p out
          cd out
          g++ -std=c++17 ../src/main.cpp -o app -static-libstdc++ -static-libgcc

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: my-app
          path: out/app

 
